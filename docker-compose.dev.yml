services:
  php:
    build:
      target: development
      dockerfile: Dockerfile.php
      args:
        USER_ID: ${SPIN_USER_ID}
        GROUP_ID: ${SPIN_GROUP_ID}
    ports:
      - "80:8080"
      - "443:8443"
    volumes:
      - "./:/var/www/html"
    environment:
      SSL_MODE: "mixed"
